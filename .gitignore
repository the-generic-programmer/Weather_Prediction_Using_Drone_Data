# Ignore all files in the models directory
models/*
# But do not ignore the directory itself
!models/

# Ignore all files in the data directory
data/*
# But do not ignore the directory itself
!data/

# Ignore all files in the waste directory
waste/*
# But do not ignore the directory itself
!waste/

